# Changeset

## 1.0.1

### Minor Changes

- Added support for resizing the iframe in the Batch Styler plugin
  - Users can now resize the plugin window by dragging the resize handle in the bottom-right corner
  - Implemented minimum and maximum size constraints to ensure usability
  - Connected to Figma's UI resize API for seamless integration
